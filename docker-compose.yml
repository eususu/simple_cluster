version: '3'
services:
    tomcatmanager:
      build:
        context: .
        args:
          conf: tomcat1/conf
          defaultWebApps: tomcat1/webapps
          warPath: tomcat1/war/OSCAR-1.0.2.war
      ports:
        - "8201:8080"
    tomcatworker:
      build:
        context: .
        args:
          conf: tomcat2/conf
          defaultWebApps: tomcat1/webapps
          warPath: tomcat2/war/OSCAR-1.0.2.war
      ports:
        - "8202:8080"
    nginx:
        image: nginx
        ports:
            - "8200:80"
        volumes:
            - ./nginx/nginx.conf:/etc/nginx/nginx.conf

        restart: always
