version: '3'
services:
    nginx:
        image: nginx
        ports:
            - "8200:80"
        volumes:
            - ./nginx/nginx.conf:/etc/nginx/nginx.conf
    was1:
        build: .
    was2:
        build: .
